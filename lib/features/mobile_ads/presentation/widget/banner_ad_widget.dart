import 'package:flutter/material.dart';
// import 'package:google_mobile_ads/google_mobile_ads.dart';
import 'package:starter_application/features/mobile_ads/presentation/logic/mobile_ad_banner.dart';

class MobAdBanner extends StatelessWidget {
   MobAdBanner({Key? key , /*required this.mobileAdBanner*/}) : super(key: key);
  // MobileAdBanner mobileAdBanner;
  @override
  Widget build(BuildContext context) {
    return Container(
      // width: mobileAdBanner.bannerAd.sizes.first.width.toDouble(),
      // height: mobileAdBanner.bannerAd.sizes.first.height.toDouble(),
      //   child: AdWidget(
            // ad: mobileAdBanner.getBanner(),
        // ),
    );
  }
}
